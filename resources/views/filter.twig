<select
        class="form-control"
        name="{{ field_type.input_name }}">

    {% for value, option in field_type.options %}
        <option value="{{ value }}" {{ value == field_type.value|first ? 'selected' }}>{{ trans(option) }}</option>
    {% endfor %}

</select>
